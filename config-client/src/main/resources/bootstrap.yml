spring:
  cloud:
    config:
      name: config
      profile: prod
#      uri: http://localhost:8091/
      label: master
      discovery:
        enabled: true
        service-id: config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/