server:
  port: 8888

spring:
  application:
    name: gateway-service-zuul

zuul:
  routes:
    api-test:
      path: /producer/**
      serviceId: demo

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/